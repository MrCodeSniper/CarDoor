<?xml version="1.0" encoding="utf-8"?>
<com.chenhong.android.carsdoor.view.DragGridView.SquareLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent" >

    <LinearLayout
        android:id="@+id/edit_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@android:color/transparent"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/icon_iv"
            android:layout_width="@dimen/layout_50"
            android:layout_height="@dimen/layout_50"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="10dp"
            android:contentDescription="@string/hello_world"
            android:scaleType="fitCenter"
            android:src="@drawable/tubiao_20" />

        <TextView
            android:id="@+id/name_tv"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="5dp"
            android:gravity="center"
            android:paddingBottom="10dp"
            android:text="@string/hello_world"
            android:textColor="#000"
            android:textSize="15sp" />
    </LinearLayout>

    <ImageView
        android:id="@+id/delet_iv"
        android:layout_width="@dimen/layout_30"
        android:layout_height="@dimen/layout_30"
        android:layout_alignRight="@id/edit_ll"
        android:layout_alignTop="@id/edit_ll"
        android:contentDescription="@string/hello_world"
        android:padding="8dp"
        android:clickable="true"
        android:scaleType="fitCenter"
        android:src="@drawable/edit_flag"
        android:visibility="visible" />

</com.chenhong.android.carsdoor.view.DragGridView.SquareLayout>